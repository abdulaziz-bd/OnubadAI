# OpenAI-compatible endpoint - used for text translate and Live conversation.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_REALTIME_MODEL=

# Live conversation needs an endpoint that actually implements the OpenAI Realtime API (WebRTC) - not every OpenAI-compatible server does.
