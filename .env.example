# OpenAI-compatible endpoint (OpenAI, vLLM, etc.) - used by text translate.
OPENAI_BASE_URL=http://localhost:8000/v1
OPENAI_API_KEY=
OPENAI_MODEL=

# Live conversation reuses OPENAI_API_KEY above but always calls the real OpenAI Realtime API - vLLM doesn't serve it.
